version: '2'

services:
  busvbot:
    container_name: busvbot
    build:
      context: ../src/BusVbot
      args:
        project: BusVbot
    expose:
      - "80"
    restart: on-failure
    networks:
      - BusVbot-network

networks: 
  BusVbot-network:
    driver: bridge
